# 🚀 Pro JSON Editor - 專業級 JSON 智慧處理工具

這是一個專為開發者打造的輕量化、純前端 JSON 編輯器。除了基本的格式化功能，它更針對開發者從 IDE (VS Code/Vim) 或 CLI (Terminal) 複製代碼時常遇到的痛點進行了深度優化。

## ✨ 強大功能

### 1. 🛠️ 智慧清理行號 (Smart Line-Number Removal)

從 IDE 或終端機複製 JSON 時，時常會不小心帶入開頭的行號數字，導致標準解析器失效。本工具能一鍵偵測並剔除每一行開頭的行號。

### 2. 🛡️ 控制字元修復 (Control Character Sanitization)

解決常見的 SyntaxError: Bad control character 錯誤。我們會自動過濾或轉換：

不換行空白 (Non-breaking space, \u00A0)

實體 Tab 字元 (轉換為空格以符合標準)

不可見的 ASCII 控制字元 (0x00-0x1F)

### 3. 🎨 即時美化與壓縮

Pretty Format: 兩格縮排，讓資料一目了然。

Minify: 移除多餘空格，用於生產環境傳輸。

### 4. 🔍 即時語法檢查

輸入時即時反饋語法狀態，若有錯誤會精確指出錯誤行數與原因。

## 📖 使用範例

情境：從終端機複製帶有行號的資料

輸入內容：

```
     1 {
     2   "id": "user-123",
     3   "data": {
     4     "name": "Gemini",
     5     "active": true
     6   }
     7 }
```

點擊「清理行號 + 格式化」後：

```json
{
  "id": "user-123",
  "data": {
    "name": "Gemini",
    "active": true
  }
}
```

## 🚀 快速開始

你可以透過以下方式部署或使用此工具：

線上使用：[GitHub Pages](https://hahaicanfly.github.io/Pro-JSON-Editor/)

單檔執行：下載 index.html 並直接用瀏覽器開啟，無需安裝任何環境。

進入 Settings > Pages 開啟 GitHub Pages 服務。

🛠️ 技術棧

React 18
Tailwind CSS: 現代化的深色模式介面。
Lucide React: 簡潔的向量圖示。
Regex Engine: 高效的文本清理算法。
